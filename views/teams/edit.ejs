
<% include ../partials/header %>
  <div class="ui two column centered grid">
    <div class="column">
      <h1>Edit your Team.</h1>
      <form action="/teams/<%= team.id %>?_method=put" method="POST">
        <div class="ui form segment">
          <div class="field">
              <label for="comments">Team Name:</label>
              <input type="text" name="team[name]" id="teamName" value="<%= team.name %>">
          </div>
          <input class="ui submit purple button" type="submit">
          <a href="/teams/<%= team.id %>">Cancel</a>
          </div>
          </div>
        </div>
      </form>
    </div>
  </div>
<% include ../partials/footer %>